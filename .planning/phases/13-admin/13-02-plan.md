---
phase: 13-admin
plan: '13-02'
type: execute
wave: 3
depends_on: ['13-01']
files_modified:
  - src/components/admin/ContentModeration.tsx
  - src/components/admin/SystemAnalytics.tsx
  - src/components/admin/AuditLogViewer.tsx
autonomous: true
requirements:
  - REQ-13-04
  - REQ-13-05
  - REQ-13-06
continuation: '.planning/phases/14-security/14-01-plan.md'
loop_check: 'npm run typecheck && npm run lint && npm run test -- --run && npm run build'
must_haves:
  truths:
    - ContentModeration renders flagged lessons with approve/reject actions
    - SystemAnalytics renders DAU, lesson completion rate, and error rate stats
    - AuditLogViewer renders filterable audit log with CSV export
  artifacts:
    - src/components/admin/ContentModeration.tsx (real implementation)
    - src/components/admin/SystemAnalytics.tsx (real implementation)
    - src/components/admin/AuditLogViewer.tsx (real implementation)
---

<objective>
Complete the admin portal with content moderation, system analytics, and audit log viewer.
Purpose: Admins need to moderate content, understand platform health, and audit all actions.
Output: 3 fully-implemented admin components.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/admin/ContentModeration.tsx
@src/components/admin/SystemAnalytics.tsx
@src/components/admin/AuditLogViewer.tsx
@src/components/ui/Badge.tsx
@src/components/ui/Button.tsx
@src/components/ui/Alert.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement ContentModeration component</name>
  <files>src/components/admin/ContentModeration.tsx</files>
  <action>
Replace stub with real component:
- Fetch lessons WHERE moderation_status = 'flagged' OR moderation_status IS NULL from lessons table
  - If lessons table doesn't have a moderation_status column: add `ALTER TABLE lessons ADD COLUMN IF NOT EXISTS moderation_status TEXT DEFAULT 'approved' CHECK (moderation_status IN ('pending', 'approved', 'rejected', 'flagged'));` as migration 0021_content_moderation_status.sql
- Render a table: Lesson Title | Course | Created By | Status | Actions
- Actions: Approve button (green), Reject button (red)
  - Approve: UPDATE lessons SET moderation_status = 'approved' WHERE id = ?
  - Reject: UPDATE lessons SET moderation_status = 'rejected' WHERE id = ?
- Status filter tabs: All | Flagged | Pending | Rejected
- Empty state: "No content requiring moderation"
- Badge colors: pending=yellow, flagged=orange, rejected=red, approved=green
  </action>
  <verify>npm run typecheck 2>&1 | grep "ContentModeration" || echo "no errors"</verify>
  <done>ContentModeration renders moderation queue with approve/reject, 0 type errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement SystemAnalytics component</name>
  <files>src/components/admin/SystemAnalytics.tsx</files>
  <action>
Replace stub with real component:
- Fetch these metrics from Supabase:
  1. DAU (daily active users): SELECT COUNT(DISTINCT student_id) FROM lesson_progress WHERE DATE(created_at) = CURRENT_DATE
  2. WAU (weekly): same but last 7 days
  3. Lesson completion rate: COUNT(completed=true) / COUNT(*) * 100 FROM lesson_progress
  4. Average session duration: AVG(duration_seconds) FROM cognitive_sessions
  5. Error rate: fetch from audit_log WHERE action = 'error' AND created_at > NOW() - INTERVAL '24h', count / total_events * 100
- Render 5 stat cards in a 3-col grid (reuse same stat card pattern as AdminDashboardPage)
- Below cards: a "Last 7 Days" section with a simple CSS bar chart of daily completions
  - One bar per day, width = completions that day / max completions * 100%
  - x-axis labels: Mon, Tue, Wed, Thu, Fri, Sat, Sun
- No charting libraries
  </action>
  <verify>npm run typecheck 2>&1 | grep "SystemAnalytics" || echo "no errors"</verify>
  <done>SystemAnalytics renders 5 stat cards + CSS weekly bar chart, 0 type errors</done>
</task>

<task type="auto">
  <name>Task 3: Implement AuditLogViewer component</name>
  <files>src/components/admin/AuditLogViewer.tsx</files>
  <action>
Replace stub with real component:
- Fetch from audit_log table (check migration 006 for actual column names)
  - Expected columns: id, user_id, action, resource_type, resource_id, metadata, created_at
- Renders a table: Timestamp | User | Action | Resource | Details
- Filter controls above table:
  - Date range: from/to date inputs (type="date")
  - Action filter: text input for action name search
  - User filter: text input for user ID search
- Pagination: 50 rows per page, Prev/Next
- CSV export (same pattern as EducatorAnalytics): downloads visible rows
- Sort by created_at DESC by default
- Timestamp formatted as local date+time string
- Details column: JSON.stringify(metadata) truncated to 80 chars with title= showing full value

All filters are applied via Supabase query params (server-side filtering), not client-side.
</action>
<verify>npm run typecheck && npm run lint</verify>
<done>AuditLogViewer renders filterable log table with CSV export, server-side filtering, 0 errors</done>
</task>
</tasks>

<verification>
- [ ] npm run typecheck — 0 errors
- [ ] npm run lint — 0 warnings
- [ ] npm run test -- --run — all tests still passing
- [ ] npm run build — clean build
- [ ] ContentModeration renders approve/reject workflow
- [ ] SystemAnalytics has CSS bar chart (no recharts/d3)
- [ ] AuditLogViewer has server-side filters + CSV export
- [ ] Migration 0021 created if lessons table needed moderation_status column
</verification>

<success_criteria>

- All 3 tasks completed
- CI gate passes
- Phase 13 (Admin Portal) is 100% implemented
- No new npm dependencies added
  </success_criteria>

<output>
After completion, create `.planning/phases/13-admin/13-02-SUMMARY.md` and update `.planning/STATE.md`:
- Phase 13: ✅ DONE
Then execute: `.planning/phases/14-security/14-01-plan.md`
</output>
