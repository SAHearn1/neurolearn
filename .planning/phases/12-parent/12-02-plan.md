---
phase: 12-parent
plan: '12-02'
type: execute
wave: 2
depends_on: ['12-01']
files_modified:
  - src/pages/ParentDashboardPage.tsx
  - src/components/parent/ParentStudentList.tsx
  - src/components/parent/ParentProgressReports.tsx
  - src/components/parent/ParentMessages.tsx
  - src/components/parent/ParentNotificationPrefs.tsx
autonomous: true
requirements:
  - REQ-12-03
  - REQ-12-04
  - REQ-12-05
  - REQ-12-06
continuation: '.planning/phases/13-admin/13-01-plan.md'
loop_check: 'npm run typecheck && npm run lint && npm run test -- --run && npm run build'
must_haves:
  truths:
    - ParentDashboardPage renders linked children with progress summaries
    - ParentProgressReports shows weekly completion charts (CSS-only, no charting library)
    - ParentNotificationPrefs toggles persist via useParentProfile.updateProfile
    - ParentMessages renders a thread list and allows sending a message
  artifacts:
    - src/pages/ParentDashboardPage.tsx (real implementation)
    - src/components/parent/ParentStudentList.tsx (real implementation)
    - src/components/parent/ParentProgressReports.tsx (real implementation)
    - src/components/parent/ParentMessages.tsx (real implementation)
    - src/components/parent/ParentNotificationPrefs.tsx (real implementation)
---

<objective>
Wire the parent portal UI to real data from hooks created in 12-01.
Purpose: Parents need to monitor their child's progress and communicate with educators.
Output: 5 fully-implemented React components.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/pages/ParentDashboardPage.tsx
@src/components/parent/ParentStudentList.tsx
@src/components/parent/ParentProgressReports.tsx
@src/components/parent/ParentMessages.tsx
@src/components/parent/ParentNotificationPrefs.tsx
@src/hooks/useParentProfile.ts
@src/hooks/useParentStudentLinks.ts
@src/components/ui/Card.tsx
@src/components/ui/Button.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement ParentDashboardPage</name>
  <files>src/pages/ParentDashboardPage.tsx</files>
  <action>
Replace stub with real implementation:
- Reads userId from authStore
- Calls useParentProfile(userId) and useParentStudentLinks(userId)
- Shows loading spinner during fetch
- If no linked students: show onboarding CTA "Link your child's account"
  - Include a simple inline form: email input + "Send Link Request" button
  - Calls requestLink(email) from hook, shows success/error feedback
- If linked students exist: render ParentStudentList component
- Tab navigation: "My Children" | "Progress Reports" | "Messages" | "Settings"
  - Use role="tablist" + role="tab" + aria-selected + aria-controls pattern
  - Each tab panel uses role="tabpanel"
- No external tab library — implement with useState for active tab

Tabs:

1. "My Children" → renders <ParentStudentList />
2. "Progress Reports" → renders <ParentProgressReports />
3. "Messages" → renders <ParentMessages />
4. "Settings" → renders <ParentNotificationPrefs />
   </action>
   <verify>npm run typecheck 2>&1 | grep "ParentDashboard" || echo "no errors"</verify>
   <done>ParentDashboardPage renders real data with tab navigation, 0 type errors</done>
   </task>

<task type="auto">
  <name>Task 2: Implement ParentStudentList component</name>
  <files>src/components/parent/ParentStudentList.tsx</files>
  <action>
Replace stub with real component:
- Props: `{ links: ParentStudentLink[], onRequestLink: (email: string) => Promise<void>, onRemoveLink: (id: string) => Promise<void> }`
- For each APPROVED link: render a Card with:
  - Student avatar (Avatar component) or initials fallback
  - Student name
  - "View Progress" link (navigate to progress tab)
  - "Remove Link" button with confirm dialog
- For each PENDING link: render a Card with "Pending approval" badge + "Cancel Request" button
- Import type ParentStudentLink from types/profile
- Empty state: "No linked children yet"
- List has role="list", cards have role="listitem"
  </action>
  <verify>npm run typecheck 2>&1 | grep "ParentStudentList" || echo "no errors"</verify>
  <done>ParentStudentList renders approved/pending links, 0 type errors</done>
</task>

<task type="auto">
  <name>Task 3: Implement ParentProgressReports component</name>
  <files>src/components/parent/ParentProgressReports.tsx</files>
  <action>
Replace stub with real component:
- Props: `{ studentIds: string[] }`
- For each student: fetch lesson_progress grouped by week (last 4 weeks)
- Render per student:
  - Student name header
  - Weekly completion bar chart using CSS (div width = completion % with bg-blue-500 color)
  - Stats row: total lessons completed, current streak, last active date
- Respect prefers-reduced-motion: if reduced motion, skip the bar width animation

CSS bar chart pattern (no library):

```tsx
<div role="img" aria-label={`${student.name} progress chart`}>
  {weeklyData.map((week) => (
    <div key={week.label} style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
      <span style={{ width: 60 }}>{week.label}</span>
      <div style={{ flex: 1, background: '#e5e7eb', borderRadius: 4 }}>
        <div
          style={{ width: `${week.pct}%`, background: '#3b82f6', height: 16, borderRadius: 4 }}
        />
      </div>
      <span>{week.count}</span>
    </div>
  ))}
</div>
```

  </action>
  <verify>npm run typecheck 2>&1 | grep "ParentProgressReports" || echo "no errors"</verify>
  <done>ParentProgressReports renders CSS-only weekly bar chart, 0 type errors</done>
</task>

<task type="auto">
  <name>Task 4: Implement ParentMessages component</name>
  <files>src/components/parent/ParentMessages.tsx</files>
  <action>
Replace stub with real component that uses Supabase for basic messaging:
- Props: `{ parentId: string }`
- Fetch messages from a `messages` table (may need to create the table — check if it exists in migrations)
  - If table doesn't exist: render a graceful "Messaging coming soon" state with a placeholder UI that has the right visual shape
  - If table exists: implement send/receive using supabase.from('messages')
- Message thread UI:
  - Left side: thread list (conversation partners)
  - Right side: message bubbles + text input + send button
- Text input uses controlled state, clears on submit
- Messages are time-stamped
- Send button has aria-label="Send message"
- New message sends via supabase.from('messages').insert(...)

If `messages` table doesn't exist, render a "coming soon" placeholder rather than failing.
Check existing hooks/migrations before implementing to use correct table name.
</action>
<verify>npm run typecheck 2>&1 | grep "ParentMessages" || echo "no errors"</verify>
<done>ParentMessages renders thread UI (real or graceful placeholder), 0 type errors</done>
</task>

<task type="auto">
  <name>Task 5: Implement ParentNotificationPrefs component</name>
  <files>src/components/parent/ParentNotificationPrefs.tsx</files>
  <action>
Replace stub with real component:
- Props: `{ profile: ParentProfile | null, onUpdate: (updates: Partial<ParentProfile>) => Promise<void> }`
- Renders two toggle switches:
  - "Email notifications" (maps to notification_email field)
  - "Push notifications" (maps to notification_push field)
- Each toggle is a styled <input type="checkbox"> with:
  - Visible label
  - aria-describedby pointing to a description span
  - Calls onUpdate({ notification_email: checked }) on change
- Shows success feedback ("Saved!") for 2 seconds after successful update
- Shows error feedback on failure
- Loading state disables both toggles during save

Pattern:

```tsx
<label htmlFor="email-toggle">Email notifications</label>
<input
  id="email-toggle"
  type="checkbox"
  checked={profile?.notification_email ?? true}
  onChange={e => handleUpdate({ notification_email: e.target.checked })}
  aria-describedby="email-toggle-desc"
/>
<span id="email-toggle-desc">Receive weekly progress summaries via email</span>
```

  </action>
  <verify>npm run typecheck && npm run lint</verify>
  <done>ParentNotificationPrefs renders 2 toggles that persist via onUpdate, 0 errors</done>
</task>
</tasks>

<verification>
- [ ] npm run typecheck — 0 errors
- [ ] npm run lint — 0 warnings
- [ ] npm run test -- --run — all tests still passing
- [ ] npm run build — clean build
- [ ] ParentDashboardPage has tab navigation with proper ARIA
- [ ] ParentProgressReports uses CSS bar chart (no charting library)
- [ ] ParentNotificationPrefs toggles persist changes
</verification>

<success_criteria>

- All 5 tasks completed
- CI gate passes
- No new npm dependencies added
- All components respect prefers-reduced-motion
- Phase 12 (Parent Portal) is 100% implemented
  </success_criteria>

<output>
After completion, create `.planning/phases/12-parent/12-02-SUMMARY.md` and update `.planning/STATE.md`:
- Phase 12: ✅ DONE
Then execute: `.planning/phases/13-admin/13-01-plan.md`
</output>
