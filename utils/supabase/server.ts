// Placeholder server helper for future SSR/edge environments.
// This app currently runs as a client-side SPA, so server client wiring
// will be implemented once server runtime requirements are finalized.
export function getSupabaseServerClient() {
  return null
}
