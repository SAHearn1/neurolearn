---
phase: 13-admin
plan: "13-01"
type: execute
wave: 2
depends_on: []
files_modified:
  - src/pages/AdminDashboardPage.tsx
  - src/components/admin/UserManagement.tsx
autonomous: true
requirements:
  - REQ-13-01
  - REQ-13-02
  - REQ-13-03
continuation: ".planning/phases/13-admin/13-02-plan.md"
loop_check: "npm run typecheck && npm run lint && npm run test -- --run && npm run build"
must_haves:
  truths:
    - AdminDashboardPage renders platform health widgets with real data
    - UserManagement renders a paginated list of all users with role badges
    - Role assignment dropdown updates profiles.role via Supabase (admin-only RLS)
    - Soft-delete sets a deleted_at timestamp rather than hard-deleting
  artifacts:
    - src/pages/AdminDashboardPage.tsx (real implementation)
    - src/components/admin/UserManagement.tsx (real implementation)
  key_links:
    - AdminDashboardPage uses Supabase service-role-equivalent (or admin-scoped) queries
    - UserManagement reads from profiles table with role column
---
<objective>
Build the admin dashboard page and user management component.
Purpose: Admins need to view platform health and manage user accounts and roles.
Output: 2 fully-implemented components.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/pages/AdminDashboardPage.tsx
@src/components/admin/UserManagement.tsx
@src/types/profile.ts
@src/components/ui/Badge.tsx
@src/components/ui/Button.tsx
@src/components/ui/Card.tsx
@src/components/ui/Spinner.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement AdminDashboardPage</name>
  <files>src/pages/AdminDashboardPage.tsx</files>
  <action>
Replace stub with real implementation:
- Reads user from authStore; redirect or show "Access denied" if role !== 'admin'
- Fetches platform health metrics from Supabase:
  - Total users: SELECT COUNT(*) FROM profiles
  - Active learners (last 7 days): SELECT COUNT(*) FROM lesson_progress WHERE created_at > NOW() - INTERVAL '7 days'
  - Total courses: SELECT COUNT(*) FROM courses
  - Total lessons completed: SELECT COUNT(*) FROM lesson_progress WHERE completed = true
- Renders 4 stat cards in a grid (use Card component)
- Below stats: tab navigation (same ARIA pattern as parent portal):
  - "Users" tab → <UserManagement />
  - "Content" tab → <ContentModeration /> (pass stub props for now — implemented in 13-02)
  - "Analytics" tab → <SystemAnalytics /> (pass stub props for now — implemented in 13-02)
  - "Audit Log" tab → <AuditLogViewer /> (pass stub props for now — implemented in 13-02)
- Stat cards show loading skeleton (just a gray div with animate-pulse class) while fetching
- Each metric has an aria-label on the stat card

Role guard pattern:
```tsx
const { user } = useAuthStore();
if (user?.role !== 'admin') {
  return <Alert type="error">Access denied. Admin role required.</Alert>;
}
```
Read authStore to understand the actual user shape.
  </action>
  <verify>npm run typecheck 2>&1 | grep "AdminDashboard" || echo "no errors"</verify>
  <done>AdminDashboardPage renders 4 stat cards + tab nav, guards against non-admin, 0 type errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement UserManagement component</name>
  <files>src/components/admin/UserManagement.tsx</files>
  <action>
Replace stub with real component:
- No props needed (admin component fetches its own data)
- Fetches all profiles from Supabase: `SELECT id, full_name, email, role, created_at, deleted_at FROM profiles ORDER BY created_at DESC`
- Client-side pagination: 20 users per page, Prev/Next buttons
- Renders an HTML table:
  - Columns: Name, Email, Role, Joined, Status (active/deleted), Actions
  - Role column: Badge component with color per role (learner=blue, educator=green, parent=yellow, admin=red)
  - Actions column: role dropdown + soft-delete button
- Role assignment:
  - <select> with options: learner, educator, parent, admin
  - On change: UPDATE profiles SET role = ? WHERE id = ?
  - Only show if the viewing user is admin and target user is not themselves
- Soft-delete:
  - Button "Deactivate" that shows confirm dialog
  - On confirm: UPDATE profiles SET deleted_at = NOW() WHERE id = ?
  - Deactivated users show "Inactive" status badge, deactivate button is disabled
- Search input at top: client-side filter by name or email
- Table has proper aria-label, th scope="col", no role= overrides needed on native table elements

Do NOT expose service_role_key in frontend code. Use Supabase anon client with admin RLS policies that allow admins to read all profiles.
  </action>
  <verify>npm run typecheck && npm run lint</verify>
  <done>UserManagement renders paginated user table with role assignment + soft-delete, 0 errors</done>
</task>
</tasks>

<verification>
- [ ] npm run typecheck — 0 errors
- [ ] npm run lint — 0 warnings
- [ ] npm run test -- --run — all tests still passing
- [ ] npm run build — clean build
- [ ] AdminDashboardPage shows role guard for non-admins
- [ ] AdminDashboardPage shows 4 stat cards
- [ ] UserManagement has client-side search + pagination
- [ ] Role assignment uses <select> not a custom dropdown component
</verification>

<success_criteria>
- Both tasks completed
- CI gate passes
- No security vulnerabilities: service_role_key not exposed in frontend
- Soft-delete is reversible (just sets deleted_at, data preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin/13-01-SUMMARY.md` and update `.planning/STATE.md`.
Then execute: `.planning/phases/13-admin/13-02-plan.md`
</output>
